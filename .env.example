# OptiBook env templates have been split into per-package files.
# See:
#   backend/.env.example   — server, database, JWT, CORS, staff gate
#   frontend/.env.example  — Vite API URL
#
# Copy each .env.example to .env inside its own directory and fill in values
# locally. Real .env files are gitignored at the repo root.
